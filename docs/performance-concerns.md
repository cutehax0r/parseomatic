# Performance concerns

Not bugs -- things that work fine on the current fixture (547MB / 1.8M lines / ~6k units) but are the first places a "super responsive UI" (the project's top priority, per `planning.md`) would start to crack as data scales up: longer raid nights, more pulls, more totems/pets, or features already on the roadmap (live log-tailing, more typed fields). Recorded here so they're tracked, not lost, even before each one gets fixed. Update this file's status column as each gets addressed.

## High impact -- most likely to actually cost a responsive UI

| # | Concern | Why it matters | Status |
|---|---|---|---|
| 1 | Debug view tables aren't virtualized -- `renderDebugLists` builds one real `<tr>` per row (~6k DOM rows today for Units/Pets/Creatures/Spells combined). | Same problem the raw view already solved, just not applied here. Tolerable today; gets janky first as unit/spell cardinality grows (longer nights, more totems). | Fixed -- converted all 9 tabs from `<table>` to the same `VirtualList` used for the raw view, backed by an in-memory `fetchRange`. |
| 2 | Raw view rows are destroy-and-recreate, not recycled -- `renderVisibleRawRows` calls `replaceChildren(...)` with a fresh node set on every range change. | More GC pressure and layout thrashing than necessary during continuous fast scrolling, which is the primary interaction this view is built for. | Fixed -- extracted `src/virtual-list.ts`, a generic row-recycling `VirtualList<T>`, and switched the raw view to it. |
| 3 | One heap allocation per event for `raw_fields: Box<[FieldSpan]>` -- 1.8M separate small allocations for the current fixture. | Allocator overhead during parse, memory overhead (per-allocation bookkeeping + fragmentation), and worse cache locality when resolving rows (pointer-chasing scattered heap blocks instead of one contiguous arena). | Fixed -- flattened into one shared arena (`raw_field_arena` + `(start,len)` ranges) in `EventStore`. |
| 4 | No client-side caching of resolved names in the raw view -- scrolling back over already-seen rows re-fetches, re-clones, and re-serializes the same ~20-40 player/pet names every time. | A raid log reuses the same small cast constantly; this is pure redundant work on both IPC payload size and Rust-side string cloning. | Fixed -- `raw_events` now sends raw intern ids (`sourceUnitId`/`targetUnitId`/`spellId`) instead of resolved strings; the frontend resolves them against the `units`/`spells` arrays it already has from `log_lists` (ids are dense and index-aligned). No per-row table lookup or string cloning left server-side at all. |
| 8 | Switching *to* the Overview view has a perceptible ~50-100ms hitch (raw↔debug has none). Overview-specific work on the switch: first-time `buildView` DOM construction (~12 widgets + the inline SVG chart), the `query_events` round trips in `paint()` (each a window scan + reduce + JSON), then the chart's Catmull-Rom SVG path over ~300-800 buckets. | raw↔debug is a pure `hidden` toggle over pre-built DOM; Overview rebuilds and re-queries, and every encounter re-selection pays the query cost again. The project's top priority is a super-responsive UI (`planning.md`). | **Partly done.** (a) landed: chart series + stat totals are one bucketed query. The players table then grew a second grouped pass (own/pet split by `(sourceOwner, sourceUnit)`) and a third (`targetUnit`, damage taken) -- back to **3 round trips**, but all fired together in one `Promise.all`, no waterfall. (c) landed frontend-side: `src/ui/query.ts` memoizes aggregated results by exact spec for the life of the loaded log (immutable event store), so re-picking a seen encounter or flipping away from Overview and back is **0 IPC / 0 scans**; `setLogData` clears it. Still open, roughly cheapest first: (b) build the Overview tree eagerly on log load, not lazily on first switch; (d) render the shell + stale values immediately, swap in fresh numbers when the query resolves; (e) **partly landed** -- `src-tauri/src/stats.rs` computes per-encounter, per-player damage/healing/taken (+ deaths, alive/active time, movement) in one parallel scan of the encounter windows (~4 ms for the fixture), lazily on first `encounter_stats()` call and cached. The Overview players table doesn't consume it *yet* (still runs its 3 grouped queries); migrating it onto `encounter_stats` drops those scans. Custom ranges keep the live path; (f) move the SVG path build off the critical path. |
| 9 | `query_events`' `kind` filter stringified per row -- `query::passes` -> `row_value(Field::Kind)` -> `LineKind::label()` -> `format!("{}_{}", …)` -> a heap `String` **per row, per clause**, then dropped. Every damage/heal query carries `where kind in [...]`. | ~2 allocs/row across the window (~100k for a 5-min pull, into the millions for a "Full log" range); pure waste on the hottest query path, growing with the selected range. | Fixed -- `query::compile` pre-resolves a `kind` `eq`/`in` clause's strings to `Vec<LineKind>` once per query (`LineKind::from_label`, an inverse of `label` via the parser's `classify`); the per-row check is now `Vec::contains` over `events.kind[row]`, no allocation. `LineKind::label()` stays for display. The same machinery (`Compiled::SourceKindIn`) now covers `sourceOwnerKind` `eq`/`in` (the chart's player/enemy split), which would otherwise `to_string()` a `UnitKind` per row. |

## Medium impact -- worth watching as things scale further

| # | Concern | Why it matters | Status |
|---|---|---|---|
| 5 | No debounce on fast/jumpy scrolling -- the rAF throttle caps at ~60 calls/sec, but a fast drag or a future "jump to row" feature could still fire many `raw_events` calls before the user settles. | Unnecessary IPC pressure during rapid scroll/seek. | Fixed -- `VirtualList` now throttles scroll-driven renders to at most one per `minRenderIntervalMs` (80ms default), with a trailing call so the final scroll position always renders shortly after the user stops. Applies to both the raw view and the debug tables, since it's in the shared class. |
| 6 | `log_lists` rebuilds its full payload (every unit/spell/zone/encounter/death/gear row, all cloned) on every call, including calls triggered by a view-only change where the underlying data hasn't changed at all. | Wasted work today; becomes the thing that has to change first if live log-tailing (already on the `planning.md` roadmap) ever lands. | Fixed -- frontend now skips the refetch when only the active view changed and the line count is unchanged. |
| 7 | `WindowLogs`'s mutex is held for the *entire* duration of row-building in `log_lists`/`raw_events`/`raw_event_count` (the `log` reference borrows from the lock guard), not just the lookup. | While one window's (possibly slow) row-building runs, any other window touching the same global map -- opening a file, closing, polling `window_info` -- blocks on the same lock, even though they're touching unrelated data. | Fixed -- shared `current_log()` helper clones the `Arc` and drops the lock immediately. |
| 10 | The Timeline widget (`src/ui/widgets/timeline-lanes.ts`) `render()` fully rebuilds the SVG (`svg.replaceChildren()` + one `<rect>` per box) and the gutter on **every** interaction -- zoom, expand/collapse, filter toggle, resize. It also draws **every** box across the whole content width, not just the ~1 pane-width that's visible; at max zoom (`MAX_PX_PER_MS = 2`) a 10-min window is a ~1.2M-px SVG holding every box. A heavy multi-DoT class over a long pull is 10k+ boxes. | A zoom step / filter toggle on a dense player rebuilds ~10k DOM nodes. Scrolling a max-zoom strip repaints a huge SVG. The project's top priority is a super-responsive UI. | **Partly done.** Landed: render() triggers other than zoom (expand / filter / resize) coalesce to one rebuild/frame (`scheduleRender`); a trackpad ⌘-wheel accumulates into one zoom/frame; per-lane sorted-box + spell-list derivation moved out of the per-render path into a cache rebuilt only on `update()`; a run of min-width slivers of the same class on one pixel column collapses to a single `<rect>`; `boxAt()` hit-test early-exits once boxes start right of the cursor (was O(all boxes in the row) per hover frame). Still open: (a) viewport virtualisation -- only build boxes within `[scrollLeft - margin, scrollLeft + paneW + margin]` and re-render (debounced) on scroll, like the raw/debug views (#1, #2); (b) on a pure zoom/scroll, translate/rescale the existing `<rect>`s instead of tearing down and rebuilding the whole tree -- only a structural change (expand / filter / new data) needs a full rebuild; (c) `processHover` / `openFilter` force a layout via `getBoundingClientRect` each call -- cache the SVG rect, invalidate on scroll/resize. |

## Low impact -- premature today, cheap to fix later if needed

- `new Date(ms)` allocated per row, per render in `formatRawTimestamp` -- almost certainly negligible until proven otherwise.
- Intern tables (`FxHashMap`) aren't pre-sized, so they rehash a few times while growing during parse. Could pre-size from a rough file-size heuristic.
- A background parse isn't cancelable if the user replaces the file in the same window mid-parse -- wasted CPU on an orphaned result, not a UI-responsiveness issue today.
- `ViewContext.get players()` (`src/ui/context.ts`) re-runs `units.filter(kind === "Player")` on every access. Nothing hot reads it yet (Overview uses `ctx.units` + resolves owners itself), but a widget looping over `ctx.players` would re-filter each time -- memoize per `logData` identity when something needs it.
- ~~`query::run_aggregate`'s grouped path allocates a `Vec<Val>` key per row~~ Fixed -- `GroupKey` inlines the 0/1/2-field cases (every real query), so the per-row key is stack-built; a `Vec` is allocated only per *group*, at emit time. 3+ group fields still fall back to `Vec`.
- `tokenizer::split_fields` returns a fresh `Vec<FieldSpan>` per line (~1.8M for the fixture). Each rayon chunk could thread one reusable scratch buffer through `parse_line` (cleared per line; `EventStore::push` already copies the needed span range into its arena), turning it into ~numcpus allocations. Contained but touches the hottest parse path -- deferred until the parse phase is actually a measured bottleneck (547MB parses in ~0.2s today).
- `movementSegments` (`src/ui/movement-segments.ts`, Timeline's Movement lane) calls `distIn(a,b)` -- an O(fixes) rescan of the whole sample array -- once per Moving segment, so O(segments x fixes). Segment count is bounded (fight length / `STAND_MIN_MS`, ~tens), so it's a few thousand ops per `paint()` today; a cumulative-distance prefix sum over the fixes would make each `distIn` O(1) if it ever matters.
- ~~`timeline::series` re-implements the `_DAMAGE`/`_HEAL` direction classification + same-ms `SWING_DAMAGE`/`_LANDED` dedup from `movement::events`~~ Fixed -- extracted to `hits::HitScanner` (`src-tauri/src/hits.rs`), a stateful per-row classifier both scans now feed row-by-row (no extra pass), so the direction logic and the SWING-twin dedup key live in exactly one place. Each caller maps the shared `Hit` onto its own result enum (`EventKind` / `InstKind`). The `UNIT_DIED`..`SPELL_RESURRECT` pairing is still open-coded in both `movement::series` and `timeline::series` (a few lines each, over their own already-fused loops).
