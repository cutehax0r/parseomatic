{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all app windows (every window shows the same kind of content, so one glob-matched capability covers them all instead of updating this file per window label)",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
